'use strict';

const register = require( './register' );
const controllers = require( './controllers' );
const routes = require( './routes' );

module.exports = {
  register,
  controllers,
  routes,
};
