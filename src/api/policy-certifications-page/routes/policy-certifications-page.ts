/**
 * policy-certifications-page router
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter(
  'api::policy-certifications-page.policy-certifications-page' as any
);
